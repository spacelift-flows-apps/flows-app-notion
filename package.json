{
  "name": "flows-app-notion",
  "version": "1.0.0",
  "description": "Notion integration app for Spacelift Flows - automate page creation, database management, and content operations",
  "type": "module",
  "dependencies": {
    "@slflows/sdk": "^0.11.0",
    "typescript": "^5.8.3"
  },
  "devDependencies": {
    "@types/node": "^24.0.7",
    "@useflows/flowctl": "^0.1.1",
    "prettier": "^3.6.2",
    "ts-node": "^10.9.2"
  },
  "scripts": {
    "setup": "./setup.sh",
    "bundle": "npx flowctl version bundle -e main.ts",
    "format": "npx prettier --write .",
    "typecheck": "npx tsc --noEmit"
  }
}
